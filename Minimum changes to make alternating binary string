class Solution:
    def minOperations(self, s: str) -> int:
        c1 = c2 = 0       
        for i, ch in enumerate(s):
            if ch != str(i % 2):
                c1 += 1
            if ch != str((i + 1) % 2):
                c2 += 1       
        return min(c1, c2)
